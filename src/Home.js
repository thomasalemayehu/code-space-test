import React from "react";
import { Link } from "react-router-dom";

const Home = () => {
  return (
    <div>
      <Link to='/pomodoro-timer'>Pomodoro Timer </Link>
    </div>
  );
};

export default Home;